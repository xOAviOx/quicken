/** @type {import('next').NextConfig} */
const nextConfig = {
  // Images are plain <img> tags pointing at public Supabase Storage URLs, so no
  // next/image remote-pattern config is needed here.
};

export default nextConfig;
